{
  "$schema": "http://json-schema.org/schema",
  "cli": "ng-cli",
  "$id": "prototyper",
  "title": "Angular Prototyper Schematic",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "Could not detect project name. Please provide the project name:"
    },
    "name": {
      "type": "string",
      "description": "The name of the component.",
      "x-prompt": "What is the name of the component you want to create?"
    },
    "prompt": {
      "type": "string",
      "description": "A description of the component to be generated by the LLM.",
      "x-prompt": "Describe the component you want to generate:"
    },
    "style": {
      "type": "string",
      "description": "The style file extension.",
      "default": "scss",
      "enum": [
        "css",
        "scss"
      ],
      "x-prompt": "What stylesheet format would you like to use?"
    }
  },
  "required": [
    "name",
    "prompt",
    "style"
  ]
}
